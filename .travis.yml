language: php
php:
  - hhvm
sudo: false

deploy:
  provider: heroku
  api_key:
    secure: "fe725629-7db6-484d-bcae-13bf55cb537c"
  on: production

before_install:
  - '[[ "$TRAVIS_PHP_VERSION" == "nightly" ]] || phpenv config-rm xdebug.ini'
  - composer self-update

install:
  - composer install